import React from 'react'

function Button({callapi}) {
    return (
        <div>

            <button onClick={callapi}>click to genarate new jock</button>

        </div>
    )
}

export default Button
